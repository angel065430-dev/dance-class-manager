# ==============================================================================
# 環境變數範例檔 — 複製為 .env.local 後填入實際值
# .env.local 不得提交到版本控制（已列於 .gitignore）
#
# 依 MASTER_SPEC.md 第 42 節與 AI_INSTRUCTIONS.md 第 30 節：
#   - 前端只能使用 VITE_ 開頭、供公開使用的環境變數
#   - 絕對不可以把以下任何一項放進前端 / .env / VITE_ 變數：
#       SUPABASE_SERVICE_ROLE_KEY
#       Database Password
#       Admin Secret
#       任何私人 API Key
#   - 這些機密只能存在於 Supabase 專案設定或後端 Edge Function 的環境變數中
# ==============================================================================

# Supabase 專案 URL（在 Supabase Dashboard > Project Settings > API 取得）
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anon public key（公開金鑰，可放前端；不是 service role key）
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# 系統顯示名稱（選填，僅作為前端預設值；正式品牌設定以資料庫 brand_settings 為準）
VITE_APP_NAME=舞蹈課程管理系統
