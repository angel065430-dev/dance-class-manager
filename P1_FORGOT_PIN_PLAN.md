# P1_FORGOT_PIN_PLAN.md

# 忘記 PIN 重設 — P1 規劃提案

> **狀態：📋 規劃提案，等待你確認。P0 Final Acceptance 通過前不會實作任何內容，這裡只是先把方案想清楚，供你在驗收 P0 的同時可以一併評估。**
> 對應 `[Phase 3 / P1]`——這是上一輪 `PHASE_STATUS_MAPPING.md` 盤點出的遺漏項目。

---

## 1. 最低限度的管理員協助 PIN 重設方案

建議分兩層，**第 0 層現在就能用、不需要等任何開發**，第 1 層才是真正寫進 P1-A 的工作：

### 第 0 層（Supabase Studio 手動操作，零開發成本，P0 期間即可作為應急方案）

管理員直接在 Supabase Studio 的 Authentication 頁面，用學生的手機號碼找到該帳號，手動重設密碼（Studio 內建功能，不需要我們寫任何程式碼）。缺點：需要給操作的人 Supabase Dashboard 存取權限（等於間接給了遠超過「重設密碼」範圍的權限，因為 Dashboard 能看到/改動整個資料庫），只適合你自己或極少數完全信任的人暫時使用，**不適合作為長期或多人操作的方式**。

我建議把這一層明確告訴你，是因為：如果 P0 上線後、P1-A 完成前就有學生忘記 PIN，你不需要枯等，可以先用這個方式應急，不會真的被「完全沒有任何重設手段」卡住。

### 第 1 層（P1-A 正式方案：App 內管理員操作 + Edge Function）

1. 管理員在後台輸入學生手機號碼，點擊「重設 PIN」。
2. 前端呼叫一個新的 Supabase **Edge Function**（例如 `admin-reset-student-pin`）——這是本專案第一次需要用到 Edge Function（先前都只有 Migration/RPC），需要额外的部署步驟（`supabase functions deploy`），這點會在正式提出 P1-A 實作計畫時另外說明部署方式。
3. Edge Function 內部：
   - 先用呼叫者的 JWT 驗證身份確實是 admin（透過既有的 `is_admin()` 邏輯查詢，不新增另一套權限判斷）。
   - 驗證通過後，**由系統隨機產生一組符合強度規則的新 6 位數 PIN**（重用 `src/utils/pin.ts` 的規則，port 一份到 Edge Function），而不是讓管理員自己輸入——原因見下方第 3 節。
   - 用 Service Role（只存在於 Edge Function 的環境變數，絕不進前端）呼叫 `auth.admin.updateUserById(studentUserId, { password: newPin })`。
   - 寫入 `audit_logs`（操作者、角色、時間、原因），**新 PIN 不寫入任何欄位**。
   - 把新 PIN 在**這一次的回應**中回傳給前端，顯示給管理員看**一次**。
4. 管理員用 LINE 或口頭把新 PIN 告知學生（系統本身不寄送、不留存）。

---

## 2. 完整自助 PIN 重設流程是否適合放在 P2？

**建議放在 P2，理由不是優先順序高低，而是技術上被「SMS OTP 是否啟用」這個既有待決策項目卡住**（`PHASE_0_AUDIT_REPORT.md` §4.3.1 第 3 點、`PHASE_STATUS_MAPPING.md` 已列）。

自助重設的本質是「在使用者還沒登入的狀態下，證明『你就是這支手機的持有人』」。目前系統唯一的身份識別是手機號碼，沒有 Email，這代表可行的驗證管道基本上只剩：

* 簡訊 OTP（發驗證碼到手機）——這正是 Phase 0 建議第一版不啟用的功能。
* 或其他一樣需要額外基礎建設的管道（例如語音驗證，成本更高，不建議）。

也就是說，只要 SMS OTP 還沒啟用，自助重設在技術上就做不出真正安全的版本（頂多做出一個「憑手機號碼就能改密碼」的版本，但那等於任何人只要知道別人的手機號碼就能接管帳號，比現在的風險更高，不能接受）。**所以自助重設放 P2 是被既有的 SMS OTP 決策連動的，不是我另外評估後降低的優先度**——如果你之後決定啟用 SMS OTP，自助重設自然可以跟著提前。

---

## 3. 具體的安全設計與權限控制

回應你特別強調的「PIN 不得以明碼儲存或顯示」，先說明我對這句話的理解，因為字面上「不得顯示」如果解讀成「連重設當下都不能讓任何人看到新 PIN」，會讓管理員協助重設在邏輯上做不到（總要有人知道新 PIN 才能告訴學生）。**我採用的解讀（請確認）**：

> 新 PIN 不得被**儲存**在任何資料庫欄位、`audit_logs`、日誌或前端狀態裡（重新整理頁面後就再也看不到）；重設當下**僅**回傳給發起這次操作、且已驗證身份是 admin 的那個請求，作為一次性、不落地的回應，管理員看到後透過系統外管道（LINE/口頭）轉告學生。這與 `PHASE_0_AUDIT_REPORT.md` §4.3.1 第 4 點原本的設計一致（「新 PIN 由管理員以口頭或 LINE 私訊等系統外管道告知學生，系統本身不寄送」），只是把「系統不寄送」的原則也套用在「不落地儲存」上。

具體設計：

* **權限控制**：Edge Function 內部驗證，不信任前端傳來的角色宣稱；驗證邏輯與既有 `is_admin()` 共用同一套判斷來源（`user_roles` 表），不另外發明第二套權限機制。
* **PIN 由系統隨機產生，不是管理員手動輸入**：避免管理員為了省事輸入 `123456` 這種弱 PIN（即使前端有檔，管理員操作的介面如果也讓他自己打字，等於多開一個繞過弱 PIN 檢查的入口）；系統產生保證一定符合現有的弱 PIN 規則。
* **絕不明碼儲存**：PIN 只存在於 Supabase Auth 自己的密碼雜湊機制（`auth.admin.updateUserById` 內部處理），本系統的任何自訂資料表都不會有這個值。
* **絕不明碼記錄**：`audit_logs` 只記錄「發生了一次重設」「誰做的」「什麼時候」「原因（如果管理員有填）」，不記錄新 PIN 本身。
* **絕不由系統寄送**：沒有 SMS/Email 基礎建設可以送出新 PIN，也刻意不做，維持「管理員以系統外管道轉告」的既有設計，降低本系統需要維運的敏感資料傳遞路徑。
* **稽核可追溯**：任何一次重設都可以在 `audit_logs` 查到是哪個管理員、什麼時候做的，供事後追查異常（例如某管理員帳號短時間內重設大量學生 PIN，屬於用人與營運層面的異常偵測，不在本次技術設計範圍內，但資料有留存供你之後視需要建立警示機制）。

---

## 待你確認

1. 第 3 節「PIN 不得儲存或顯示」的解讀（僅一次性回傳給發起重設的管理員、不落地儲存）是否符合你的預期？
2. 第 0 層（Supabase Studio 手動操作）作為 P0～P1-A 完成前的應急方案，是否可以先讓你（或指定的人）知道這個作法，以防真的有學生在 P1-A 完成前忘記 PIN？
3. 第 1 層（Edge Function 方案）排入 P1-A，需要在正式開始前另外說明 Edge Function 的部署方式（這是本專案第一次用到，`supabase functions deploy` 需要 Supabase CLI 且可能需要連結到你的正式 Supabase 專案），到時候會像 Phase 2 Final Acceptance 一樣，明確區分「我能做的」與「需要你在本機/Dashboard 操作的」。

這份規劃本身不會被實作，等你完成 P0 Final Acceptance 並確認以上三點後，才會排進 P1-A 的正式實作計畫。

---

## 確認記錄（使用者回覆）

> 以下逐項記錄你的回覆，作為後續 P1-A 正式實作前的既定依據，不再重新討論已確認的部分。

### 1. PIN 不得儲存或顯示 — ✅ 已確認

同意第 3 節的解讀。額外明確要求（納入後續 Edge Function 實作規格，不會再變動）：

* 新 PIN 不得寫入任何資料表。
* 不得寫入 `audit_logs`（原方案即是如此，這裡再次明確定調）。
* 不得保留在前端狀態（不可留在 store／localStorage／頁面重整後可再次取得）。
* 不得在任何一般管理頁面或 API 被再次查詢、顯示。
* 僅能在重設成功當下，一次性回傳給已通過權限驗證的發起管理員。

實作時會確保：資料庫、Log、Audit Log 或一般 API 皆無法查回 PIN 明碼——這是 P1-A 正式實作與驗收時的檢查項之一，不是可自由調整的細節。

### 2. P0～P1-A 前的應急方案（Supabase Studio 手動重設）— ✅ 已確認

可以作為少量個案的應急處理方案，附帶條件：

* 需要有明確記錄的操作步驟（正式排入 P1-A 實作前，會先整理一份簡短的操作說明文件）。
* 僅限少數指定且可信任的管理人員操作。
* 不將 Supabase 高權限帳號提供給一般行政人員。
* 明確定位為暫時方案，不取代正式的 Edge Function PIN 重設功能。

### 3. Edge Function 部署說明 — ✅ 已確認，附加要求

同意將 Edge Function 正式部署納入 P1-A。**在開始正式部署前**，會先提供一份獨立的說明文件，內容至少涵蓋：

1. Edge Function 的用途與架構說明。
2. 哪些程式碼／檔案由 Claude 完成。
3. 哪些步驟需要你在本機執行。
4. 哪些步驟需要登入 Supabase Dashboard 或連結正式 Supabase Project。
5. 正式環境與 Local Supabase 的差異。
6. 完整、可逐步執行的部署與驗收流程。

原則比照 Phase 1／Phase 2 Final Acceptance：不會宣稱執行了實際上只有你能在你的電腦或正式 Supabase Project 上執行的操作。

### 額外確認：優先順序

你重申並確定了以下順序，這會是後續所有回報依循的順序，不會因為忘記 PIN 功能而打亂：

```text
P0 Final Acceptance
        ↓
確認學生可以實際完成期課報名
        ↓
Phase 3 / P1-A 必要安全與帳號功能
        ↓
正式 Edge Function PIN 重設
        ↓
Phase 4 / P1 管理功能補強
```

**現況：仍在等待你完成 P0 Final Acceptance 的本機驗收並回報結果。** 三項確認事項已記錄在案，但不會因此提前開始 P1-A 的任何程式碼或 migration；`REGISTRATION_MVP_P0_COMPLETION_REPORT.md` 的驗收結果回報，仍是下一步實作的觸發點。

---

# End of P1_FORGOT_PIN_PLAN.md
